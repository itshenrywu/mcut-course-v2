# API 主機 (含 protocol, 結尾不要斜線), 前端 runtime 與 build 期共用
# 留空時 dev / preview 會把 /api /auth /og 反向代理到 http://localhost:10000
API_BASE=

# 站台網址 (含 protocol, 結尾不要斜線), 用來產生 sitemap.xml 的 <loc> 與轉址頁的 canonical
# 留空就不產生 sitemap, 也不注入 canonical, 避免寫出錯誤的絕對網址
SITE_BASE=
